import QuizView from 'src/sections/quiz/quiz-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Take a quiz',
};

export default function Quiz() {
  return <QuizView />;
}
